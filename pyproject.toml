[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "cpr-field-clustering"
version = "0.1.0"
description = "Public-safe field CPR patient clustering pipeline"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["src"]
